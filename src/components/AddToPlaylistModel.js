import React from 'react'

import '../styles/AddToPlaylistModel.scss'

export const AddToPlaylistModel = () => {
    return (
        <div className='model AddToPlaylistModel'>
            this playlist models
        </div>
    )
}